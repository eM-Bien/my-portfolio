@AGENTS.md

@docs/scena.md

## Zasady dla tego repo

- Odpowiadaj po polsku, zwięźle. Bez tłumaczenia podstaw.
- `AGENTS.md` jest przepisywany przez `next dev` — nie trzymać tam nic własnego.
- Przed zmianą liczb w `.apple` (Scene.module.scss) przeczytaj sekcję „Jabłko" w `docs/scena.md`.
  Są ze sobą powiązane i zmiana jednej odsłania dziurę w dłoni.
- Po zmianach w scenie: `npm run build`, a potem sprawdź render — same testy typów
  nie wyłapią tego, że coś zniknęło z kadru.
